[package]
name = "fast-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
log = "0.4.29"
bitflags = "2.11.0"
serde = { version = "1", features = ["derive"], optional = true }

[dev-dependencies]
env_logger = "0.11.9"

[features]
serde = ["dep:serde"]
